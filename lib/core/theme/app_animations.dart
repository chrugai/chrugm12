abstract class AppAnimations {
  static const Duration navigationTransition = Duration(milliseconds: 200);
  static const Duration modalOpen = Duration(milliseconds: 300);
  static const Duration modalClose = Duration(milliseconds: 200);
  static const Duration bottomSheetOpen = Duration(milliseconds: 300);
  static const Duration bottomSheetClose = Duration(milliseconds: 200);
  static const Duration buttonPress = Duration(milliseconds: 100);
  static const Duration collapseExpand = Duration(milliseconds: 200);
  static const Duration toastFadeIn = Duration(milliseconds: 200);
  static const Duration toastFadeOut = Duration(milliseconds: 200);
  static const Duration toastDisplayDuration = Duration(seconds: 3);
  static const Duration cardReorderSnap = Duration(milliseconds: 150);
  static const double buttonScaleFactor = 0.95;
}
